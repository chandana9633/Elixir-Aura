<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Roboto', sans-serif;
            background-color: #eaf8e4;
            color: #2e7d32; 
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
        }
        .success-container {
            text-align: center;
            background-color: #388e3c; 
            color: white;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            width: 100%;
            opacity: 0;
            animation: fadeIn 1s ease-out forwards;
        }
        .success-container h1 {
            font-size: 36px;
            margin-bottom: 20px;
        }
        .success-container p {
            font-size: 18px;
            margin-bottom: 30px;
        }
        .success-container .order-id {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 30px;
        }
        .button {
            background-color: #4caf50; 
            color: white;
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            transition: background-color 0.3s;
        }
        .button:hover {
            background-color: #45a049;
        }

        .checkmark {
            width: 80px;
            height: 80px;
            border: 5px solid #ffffff;
            border-radius: 50%;
            margin-bottom: 20px;
            display: inline-block;
            position: relative;
            animation: checkmarkAnimation 1s ease-in-out forwards;
        }

        .checkmark::after {
            content: '';
            position: absolute;
            top: 16px;
            left: 32px;
            width: 10px;
            height: 20px;
            border: solid #ffffff;
            border-width: 0 5px 5px 0;
            transform: rotate(45deg);
            opacity: 0;
            animation: checkAnimation 0.6s ease-in-out 1s forwards;
        }

        @keyframes checkmarkAnimation {
            0% {
                transform: scale(0);
            }
            50% {
                transform: scale(1.2);
            }
            100% {
                transform: scale(1);
            }
        }

        @keyframes checkAnimation {
            0% {
                opacity: 0;
                transform: rotate(45deg);
            }
            100% {
                opacity: 1;
                transform: rotate(45deg) translate(0, 0);
            }
        }

        @keyframes fadeIn {
            0% {
                opacity: 0;
            }
            100% {
                opacity: 1;
            }
        }
    </style>
</head>
<body>

    <div class="success-container">
        <div class="checkmark"></div>
        <h1>Order Placed Successfully!</h1>
        <p>Your order has been placed successfully. Thank you for shopping with us!</p>
        <div class="order-id">Order ID: <span id="order-id"><%=orderId%></span></div>
        <a href="/orders" class="button">Back to Orders</a>
    </div>

</body>
</html>
